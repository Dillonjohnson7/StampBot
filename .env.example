# Copy to .env and fill in. Loaded by your shell / LeRobot as needed.

# Hugging Face — needed to push/pull datasets and policies.
# Create a token at https://huggingface.co/settings/tokens
HF_TOKEN=

# Weights & Biases — optional, only if policy.wandb: true in the config.
WANDB_API_KEY=
